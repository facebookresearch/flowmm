run:
  dir: ./runs/${model.target_distribution}-${hydra:runtime.choices.data}/${now:%Y-%m-%d}/${now:%H-%M-%S}/${hydra:runtime.choices.model}-${hydra:runtime.choices.vectorfield}-${generate_id:}-${oc.env:SLURM_JOB_ID,0}
